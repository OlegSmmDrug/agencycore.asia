# Модуль Унифицированной Аналитики

## Обзор

Модуль **Унифицированной Аналитики** предоставляет комплексный анализ всех бизнес-процессов агентства с акцентом на **связи между блоками** и **динамику по месяцам**.

## Ключевые возможности

### 1. Временные ряды метрик
- Динамика всех показателей по месяцам
- Тренды роста/падения
- Сравнение периодов

### 2. Связи между блоками
Модуль показывает как разные части системы влияют друг на друга:

```
Клиенты → Проекты → Контент → Финансы
    ↓         ↓         ↓         ↓
Конверсия  Команда  Активность  Прибыль
```

### 3. Корреляционный анализ
- **Клиенты ↔ Проекты**: конверсия лидов в проекты
- **Контент ↔ Доход**: влияние публикаций на выручку
- **Команда ↔ Проекты**: эффективность загрузки
- **Проекты ↔ Выручка**: монетизация

### 4. Метрики эффективности
- **Проектов на человека**: загрузка команды
- **Контент на проект**: интенсивность работы
- **Доход на сотрудника**: производительность

## Структура данных

### Основные метрики по месяцам:

```typescript
interface MonthlyMetrics {
  month: string;           // Месяц
  new_projects: number;    // Новые проекты
  active_projects: number; // Активные проекты
  new_clients: number;     // Новые клиенты
  won_clients: number;     // Выигранные сделки
  publications: number;    // Публикации контента
  income: number;          // Выручка
  expenses: number;        // Расходы
  tasks_completed: number; // Выполненные задачи
  team_size: number;       // Размер команды
  avg_project_budget: number; // Средний бюджет проекта
}
```

### Корреляции:

```typescript
interface CorrelationData {
  metric1: string;      // Первая метрика
  metric2: string;      // Вторая метрика
  correlation: number;  // Коэффициент корреляции (-1 до 1)
  insight: string;      // Интерпретация связи
}
```

## Вкладки модуля

### 1. Обзор (Overview)
- **4 KPI карточки** с процентом роста
- **График динамики** ключевых метрик
- **Финансовая динамика**: доход vs прибыль

### 2. Рост (Growth)
- **Рост клиентской базы**: area chart новых клиентов и проектов
- **Конверсия и выручка**: связь между конверсией лидов и доходом

### 3. Эффективность (Efficiency)
- **Проектов на человека**: загрузка команды
- **Контент на проект**: интенсивность контент-маркетинга
- **Доход на сотрудника**: производительность персонала

### 4. Связи (Correlations)
- **Карточки корреляций** с процентами связи
- **Визуальные индикаторы** силы связи:
  - 🟢 Зеленый > 70%: сильная связь
  - 🟡 Желтый 40-70%: средняя связь
  - ⚪ Серый < 40%: слабая связь

## SQL функции

### get_unified_analytics
Основная функция получения метрик:

```sql
SELECT * FROM get_unified_analytics(
  'organization_id'::uuid,
  '2025-01-01'::date  -- начальная дата
);
```

### get_monthly_stats_fallback
Упрощенная версия на случай ошибок:

```sql
SELECT * FROM get_monthly_stats_fallback(
  'organization_id'::uuid
);
```

## Интеграция в код

### Использование компонента:

```typescript
import UnifiedAnalytics from './components/UnifiedAnalytics';

// В App.tsx или другом компоненте:
<UnifiedAnalytics organizationId={currentUser.organizationId} />
```

### Настройка периода:

Пользователь может выбрать период:
- **3 месяца** (по умолчанию)
- **6 месяцев**
- **12 месяцев**
- **Все время**

## Примеры использования

### Анализ роста агентства
1. Откройте вкладку **"Рост"**
2. Посмотрите динамику новых клиентов
3. Проверьте конверсию в проекты
4. Оцените тренд выручки

### Оптимизация команды
1. Откройте вкладку **"Эффективность"**
2. Проверьте загрузку команды (проектов на человека)
3. Сравните с доходом на сотрудника
4. Найдите узкие места

### Поиск точек роста
1. Откройте вкладку **"Связи"**
2. Найдите метрики с сильной корреляцией
3. Определите где улучшения дают максимальный эффект

## Интерпретация корреляций

### Сильная положительная корреляция (> 0.7)
Метрики растут вместе. Улучшение одной → улучшение другой.

**Пример**: Новые клиенты ↔ Новые проекты = 0.85
- Значит: каждый новый клиент приводит к новому проекту
- Действие: фокус на привлечение клиентов

### Средняя корреляция (0.4 - 0.7)
Есть связь, но не абсолютная.

**Пример**: Публикации ↔ Доход = 0.55
- Значит: контент влияет на доход, но не единственный фактор
- Действие: улучшить качество контента, не только количество

### Слабая корреляция (< 0.4)
Метрики слабо связаны или независимы.

**Пример**: Размер команды ↔ Доход = 0.3
- Значит: просто увеличение команды не гарантирует рост дохода
- Действие: фокус на качество найма и продуктивность

## Roadmap

### Планируемые улучшения:

1. **Прогнозирование**
   - Предсказание метрик на следующий месяц
   - ML-модели для трендов

2. **Сегментация**
   - Аналитика по типам клиентов
   - Аналитика по направлениям услуг

3. **Benchmarking**
   - Сравнение с индустрией
   - Best practices рекомендации

4. **Alerts**
   - Автоматические уведомления о аномалиях
   - Предупреждения о падении метрик

5. **Экспорт**
   - PDF отчеты
   - Excel дашборды
   - API для интеграций

## Технические детали

### База данных
- PostgreSQL с JSONB для метрик
- Индексы на date полях для быстрых агрегаций
- RLS для изоляции организаций

### Frontend
- React + TypeScript
- Recharts для визуализации
- Real-time обновления через Supabase

### Производительность
- Кеширование на уровне БД
- Pagination для больших датасетов
- Оптимизация SQL запросов

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте Supabase логи функций
3. Убедитесь что у пользователя есть доступ к модулю "analytics"

## Changelog

### v1.0.0 (2026-02-01)
- Первая версия модуля
- 4 вкладки аналитики
- Корреляционный анализ
- Временные ряды
- SQL функции для агрегации
