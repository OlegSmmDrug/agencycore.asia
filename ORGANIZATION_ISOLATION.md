# Изоляция данных между организациями

## Статус изоляции

### ✅ Полностью защищено
- **projectService** - все методы фильтруют по organization_id
- **clientService** - все методы фильтруют по organization_id
- **taskService** - фильтрует через JOIN с projects
- **userService** - полностью изолировано
- **transactionService** - фильтрует через JOIN с clients
- **noteService** - полностью изолировано по organization_id
- **calculatorService** - все методы фильтруют по organization_id
- **serviceService** - все методы фильтруют по organization_id (База знаний)
- **whatsappService** - чаты и сообщения фильтруются по organization_id
- **greenApiService** - добавляет organization_id при отправке сообщений
- **wazzupService** - добавляет organization_id при отправке сообщений

### ⚠️ Требует внимания
- **roadmapService** - глобальные шаблоны Level1 (общие для всех), но project_roadmap_stages изолированы через проекты
- **contractTemplateService** - шаблоны контрактов НЕ фильтруются по organization_id (общие для всех)
- **WebHooks** - НЕ добавляют organization_id при создании входящих сообщений и новых клиентов (требует доработки с default organization)

### 🔍 Требует проверки
Следующие сервисы требуют ревью:
- salarySchemeService
- payrollService
- kpiSuggestionService
- activityLogService
- notificationService
- documentService
- guestAccessService

## Принципы изоляции

1. **Каждый запрос на чтение** должен фильтроваться по `organization_id`
2. **Каждое создание** должно автоматически добавлять `organization_id`
3. **Каждое обновление/удаление** должно проверять `organization_id`
4. **Для связанных таблиц** используется JOIN для фильтрации через родительские таблицы

## Централизованная утилита

Создана утилита `/utils/organizationContext.ts`:
- `getCurrentOrganizationId()` - получить ID текущей организации
- `requireOrganizationId()` - получить или выбросить ошибку
- `getCurrentUserId()` - получить ID текущего пользователя
- `requireUserId()` - получить или выбросить ошибку

## База данных

- RLS отключен для всех таблиц
- Фильтрация происходит на уровне приложения через organization_id
- Все основные таблицы имеют поле organization_id

## Известные ограничения

1. **WhatsApp WebHooks** - входящие сообщения от новых клиентов создаются без organization_id
   - Решение: требуется настройка default organization для webhooks

2. **Шаблоны контрактов** - доступны всем организациям
   - Причина: шаблоны могут быть общими для всей системы
   - Решение: добавить organization_id при необходимости изоляции

3. **Roadmap Level1 шаблоны** - общие для всех организаций
   - Причина: базовые этапы универсальны
   - Level2 stages изолированы через проекты

## Тестирование

Для проверки изоляции:
1. Создайте 2 разные организации
2. В каждой создайте:
   - Клиентов
   - Проекты
   - Задачи
   - Заметки
   - Услуги в калькуляторе
   - Записи в базе знаний
3. Убедитесь, что данные одной организации не видны в другой
4. Проверьте, что нельзя редактировать данные другой организации через direct ID
5. Отправьте WhatsApp сообщения от каждой организации и проверьте изоляцию

## Что исправлено

### Сервисы с добавленной изоляцией:
1. **calculatorService** - калькулятор услуг
2. **serviceService** - база знаний (услуги)
3. **whatsappService** - чаты и сообщения WhatsApp
4. **greenApiService** - отправка сообщений через Green API
5. **wazzupService** - отправка сообщений через Wazzup
6. **noteService** - заметки
7. **transactionService** - финансовые транзакции
8. **userService** - создание пользователей

### Все методы обновлены:
- ✅ getAll() - фильтрует по organization_id
- ✅ create() - автоматически добавляет organization_id
- ✅ update() - проверяет organization_id
- ✅ delete() - проверяет organization_id
- ✅ realtime subscriptions - фильтруют по organization_id
